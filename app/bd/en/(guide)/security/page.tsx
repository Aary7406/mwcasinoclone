import { SecurityContent } from '@/components/generated';
import GuideShell from '@/components/shell/GuideShell';

export default function Page() {
  return (
    <GuideShell>
      <SecurityContent />
    </GuideShell>
  );
}
