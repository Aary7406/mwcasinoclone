import { AmlPolicyContent } from '@/components/generated';
import GuideShell from '@/components/shell/GuideShell';

export default function Page() {
  return (
    <GuideShell>
      <AmlPolicyContent />
    </GuideShell>
  );
}
