import { PrivacyPolicyContent } from '@/components/generated';
import GuideShell from '@/components/shell/GuideShell';

export default function Page() {
  return (
    <GuideShell>
      <PrivacyPolicyContent />
    </GuideShell>
  );
}
